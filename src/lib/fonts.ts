// Shared font-family stacks matching the CSS variables wired up in layout.tsx /
// globals.css. Import these instead of re-declaring the strings inline.
export const JOST_STACK = "var(--font-jost), system-ui, sans-serif";
export const LIVVIC_STACK = "var(--font-livvic), system-ui, sans-serif";
